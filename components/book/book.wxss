/* components/book/book.wxss */
.book-wrapper{
  width: 100%;
  height: 200rpx;
  display: flex;
  align-items: center;
}
.book-image {
  height: 150rpx;
  width: 100rpx;
  margin: 0 20rpx;
}
.book-name {
  font-size: 40rpx;
}
.book-author {
  font-size: 32rpx;
  margin: 10rpx 0
}
.book-des {
  width:500rpx;
  color: #cccccc;
  font-size: 32rpx;
  overflow: hidden;
  text-overflow:ellipsis;
  white-space: nowrap;
}